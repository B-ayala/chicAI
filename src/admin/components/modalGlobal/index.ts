export {
  renderActions,
  renderProductStock,
  renderStatusTag,
  statusColors,
  Table,
  TableBuilder,
} from './table';

// Re-export de tipos
export type { Column, StatusType, TableBuilderConfig, TableProps } from './table';

// Re-export de componentes de estilo reutilizables
export {
  ActionLink,
  Card,
  CardLabel,
  CardList,
  CardRow,
  CardValue,
  StatusTag,
  StyledTable,
  TableCell,
  TableHead,
  TableRow,
  TableWrapper,
} from './styled';
